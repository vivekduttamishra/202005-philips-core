@model ConceptArchitect.BookManagement.Author

<h2>About @Model.Name</h2>

@*<a href="/author/list" class="btn btn-primary">Back To List</a>*@

@Html.ActionLink("Back To List", "List", null,new { @class="btn btn-primary btn-sm"})

<table class="table table-striped table-hover">
    <tr>
        <th>Name</th>
        <td>@Model.Name</td>
    </tr>
    <tr>
        <th>Biograpy</th>
        <td>@Model.Biography</td>
    </tr>
    <tr>
        <th>Email</th>
        <td>@Model.Email</td>
    </tr>

    <tr>
        <th>Birth Date</th>
        <td>@Model.BirthDate</td>
    </tr>

    <tr>
        <th>Death Date</th>
        <td>@Model.DeathDate</td>
    </tr>

    <tr>
        <th>Photograph</th>
        <td>@Model.Photograph</td>
    </tr>
    <tr>
        <th>Age</th>
        <td>@Model.Age</td>
    </tr>
    </table">
